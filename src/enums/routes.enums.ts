export enum BOTTOM_TAB_ROUTES {
  RESTAURANTS = 'restaurants',
  MAP = 'map',
  ACCOUNT = 'account',
}

export enum ROOT_ROUTES {
  RESTAURANT_LIST_SCREEN = 'restaurant-list-screen',
}

export enum AUTH_ROUTES {
  AUTH_MAIN_SCREEN = 'auth-main-screen',
  SIGN_IN_SCREEN = 'sign_in-screen',
  SIGN_UP_SCREEN = 'sign-up-screen',
}
