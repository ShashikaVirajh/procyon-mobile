import { colors } from './colors.theme';
import { fonts, fontSizes } from './fonts.theme';
import { lineHeights, spaces } from './spaces.theme';

export const theme = {
  colors,
  fonts,
  fontSizes,
  lineHeights,
  spaces,
};
