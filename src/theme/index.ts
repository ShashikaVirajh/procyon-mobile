import { colors } from './colors.theme';
import { fonts, fontSizes, fontWeights } from './fonts.theme';
import { lineHeights, spaces } from './spaces.theme';

export const theme = {
  colors,
  fonts,
  fontSizes,
  fontWeights,
  lineHeights,
  spaces,
};
